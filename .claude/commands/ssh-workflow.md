# SSH Workflow

Run the SSH management layer skill for image validation.

## Instructions

1. Read `.claude/skills/ssh-management-layer/SKILL.md`
2. Follow the 5-step pipeline (input, preflight, plan, execute, report)
3. Reference step files in `references/` for details
